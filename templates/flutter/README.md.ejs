# <%= projectName %> - Flutter App

Aplicación Flutter generada automáticamente desde diagrama UML.

## 📱 Características

- ✅ CRUD completo para todas las entidades
- ✅ Interfaz Material Design moderna y responsiva
- ✅ 🎙️ **Autocompletado de Formularios por Voz con IA**: dicta los datos de cualquier entidad y la IA rellenará automáticamente los campos y deducirá coherentemente los no mencionados.
- ✅ Consumo de API REST (Spring Boot Backend para CRUD + NestJS para IA)
- ✅ Validaciones de formularios
- ✅ Manejo de errores y estados de carga

## 🚀 Requisitos Previos

- [Flutter SDK](https://flutter.dev/docs/get-started/install) (>= 3.0.0)
- [Android Studio](https://developer.android.com/studio) o [VS Code](https://code.visualstudio.com/)
- Backend Spring Boot corriendo en `http://localhost:8080` (CRUD)
- Backend NestJS corriendo en `http://localhost:3002` (Servicio de IA para voz)

## 📦 Instalación y Configuración

1. **Instalar dependencias:**
   ```bash
   flutter pub get
   ```

2. **Permisos de Micrófono (Android / iOS):**

   - **Android** (`android/app/src/main/AndroidManifest.xml`):
     Dentro de la etiqueta `<manifest>` agrega:
     ```xml
     <uses-permission android:name="android.permission.RECORD_AUDIO" />
     <uses-permission android:name="android.permission.INTERNET" />
     <uses-permission android:name="android.permission.BLUETOOTH" />
     <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
     <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
     <queries>
         <intent>
             <action android:name="android.speech.RecognitionService" />
         </intent>
     </queries>
     ```

   - **iOS** (`ios/Runner/Info.plist`):
     Dentro de `<dict>` agrega:
     ```xml
     <key>NSMicrophoneUsageDescription</key>
     <string>Esta aplicación requiere acceso al micrófono para el dictado por voz.</string>
     <key>NSSpeechRecognitionUsageDescription</key>
     <string>Esta aplicación requiere reconocimiento de voz para autocompletar formularios.</string>
     ```

3. **Conexión con el Backend (Spring Boot e IA):**

   La app incluye auto-recuperación y un botón de configuración ⚙️ en la pantalla principal.

   **Opción A: Teléfono físico conectado por cable USB (Recomendada):**
   Ejecuta en la terminal de tu PC:
   ```bash
   adb reverse tcp:8080 tcp:8080
   adb reverse tcp:3002 tcp:3002
   ```
   ¡Y listo! La app se comunicará por cable USB con tu Spring Boot de forma directa.

   **Opción B: Teléfono físico conectado por Wi-Fi:**
   En la app móvil toca el botón ⚙️ en la barra superior de la pantalla principal e ingresa la IP local de tu PC (ej: `192.168.1.35`).

   **Opción C: Emulador oficial de Android:**
   La app detecta automáticamente el emulador y se conecta a `http://10.0.2.2:8080`.

4. **Ejecutar la app:**

   ```bash
   flutter run
   ```

## 📂 Estructura del Proyecto

```
lib/
├── main.dart                 # Punto de entrada
├── config/
│   └── api_config.dart       # Configuración de API
├── models/                   # Modelos de datos
<% classes.forEach(function(cls) { %>
│   ├── <%= cls.className.toLowerCase() %>.dart
<% }); %>
├── services/                 # Servicios API
│   ├── api_service.dart      # Cliente HTTP base
<% classes.forEach(function(cls) { %>
│   ├── <%= cls.className.toLowerCase() %>_service.dart
<% }); %>
└── screens/                  # Pantallas
    ├── home_screen.dart      # Dashboard principal
<% classes.forEach(function(cls) { %>
    └── <%= cls.className.toLowerCase() %>/
        ├── <%= cls.className.toLowerCase() %>_list_screen.dart
        └── <%= cls.className.toLowerCase() %>_form_screen.dart
<% }); %>
```

## 🎯 Entidades Disponibles

<% classes.forEach(function(cls) { %>
### <%= cls.className %>
- **Endpoint:** `/api/<%= cls.pluralName %>`
- **Campos:**
<% cls.attributes.filter(attr => !attr.isRelation).forEach(function(attr) { %>
  - `<%= attr.name %>` (<%= attr.type %>)<%= attr.nullable === false ? ' *requerido*' : '' %>
<% }); %>
<% }); %>

## 🔧 Configuración de Red

### Android Emulator
```dart
// lib/config/api_config.dart
static const String baseUrl = 'http://10.0.2.2:8080';
```

### iOS Simulator
```dart
static const String baseUrl = 'http://localhost:8080';
```

### Dispositivo Físico
1. Obtén tu IP local:
   - **Windows:** `ipconfig`
   - **Linux/Mac:** `ifconfig`

2. Actualiza la configuración:
   ```dart
   static const String baseUrl = 'http://TU_IP:8080';
   ```

## 🧪 Testing

```bash
flutter test
```

## 📱 Build para Producción

### Android
```bash
flutter build apk --release
```

### iOS
```bash
flutter build ios --release
```

## 🐛 Troubleshooting

### Error de conexión al backend
- ✅ Verifica que el backend esté corriendo en `http://localhost:8080`
- ✅ Revisa la configuración de `api_config.dart`
- ✅ Si usas emulador Android, usa `10.0.2.2` en lugar de `localhost`

### Error de dependencias
```bash
flutter clean
flutter pub get
```

## 📚 Recursos

- [Flutter Documentation](https://flutter.dev/docs)
- [Material Design](https://material.io/design)
- [HTTP Package](https://pub.dev/packages/http)

---
**Generado automáticamente** desde diagrama UML
